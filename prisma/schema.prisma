generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Sample table
model Sample {
  id    Int    @id @default(autoincrement())
  name  String
  price Float
}
